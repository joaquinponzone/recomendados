/** Puntos al publicar una recomendación (autor). */
export const REPUTATION_PER_RECOMMENDATION = 5;

/** Puntos al emitir el primer voto sobre una recomendación (por par usuario–recomendación). */
export const REPUTATION_PER_VOTE = 1;
